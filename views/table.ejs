<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-KK94CHFLLe+nY2dmCWGMq91rCGa5gtU4mk92HdvYe+M/SXH301p5ILy+dN9+nJOZ" crossorigin="anonymous">
    <style>
        * {
            font-family: 'Gill Sans', 'Gill Sans MT', Calibri, 'Trebuchet MS', sans-serif;
        }

        a {
            letter-spacing: 1px;
        }

        a:hover {
            opacity: 0.6;
            transition: 0.3s;
        }

        strong {
            font-size: x-large;
            letter-spacing: 2px;
        }

        td {
            cursor: pointer;
            letter-spacing: 1px;
            font-size: 20px;
        }

        .add {
            background-color: white;
            border-radius: 3px;
            color: black;
            border: 1px solid black;
            margin: 2px;
            margin-right: 51vw;

            padding-left: 15px;
            padding-right: 15px;
        }

        .add:hover {
            background-color: black;
            color: white;
            transition: 0.3s;
        }

        .navbar-nav {
            margin-left: 79vw;
        }

        th,
        td {
            padding: 15px;
            color: black;
            background-color: aliceblue;
            border: solid 1px black;
            /* border-radius: 3px; */
        }

        th {
            font-size: large;
            letter-spacing: 2px;
            text-align: center;
            border: none;
        }

        tr:hover {
            background-color: crimson;
            transition: 0.3s;
            color: rgb(86, 86, 233);
        }

        body {
            /* background-color: rgb(7, 27, 50); */
            background-image: url('https://images.unsplash.com/photo-1476842634003-7dcca8f832de?ixlib=rb-4.0.3&ixid=M3wxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8fA%3D%3D&auto=format&fit=crop&w=1170&q=80');
            background-size: cover;
        }

        button {
            background-color: transparent;
            border: none;
            float: right;
            color: green;
            opacity: 0.6;
        }

        button:hover {
            color: rgb(190, 4, 4);
            transition: 0.3s;
            opacity: 1;
            border-radius: 2px;
            /* transform: rotateZ('20deg'); */
            user-select: none;
        }

        h1 {
            color: white;
            text-align: center;
        }

        table {
            margin-top: 5%;
        }

        .containe {
            justify-content: center;
            display: flex;
            margin: auto;
        }

        tr:hover {
            background-color: black;
            color: white;
            transition: 0.3s;
        }

        td {
            padding-left: 25px;
            padding-right: 25px;
            word-spacing: 2px;
            text-transform: capitalize;
        }

        .x:hover {
            /* color: white; */
            transition: 0.3s ease-in-out;
            /* padding-left: 40px; */
            color: green;
            /* transform: translateX(20px); */
            background-color: rgb(214, 214, 214);
        }
        /* .y:hover{
            transform: scale(1.5);
        } */
        #heading {
            font-size: 20px;
            letter-spacing: 3px;
            user-select: none;
        }
    </style>
    <title>Document</title>
</head>

<body>
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
        <a class="navbar-brand mx-3 " href="/"><strong>Todo</strong></a>
        <div class="collapse navbar-collapse" id="x">
            <ul class="navbar-nav">
                <li class="nav-item active">
                    <a class="nav-link active" href="/">Home</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link active" href="/getSongs">TodoList</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link active" href="https://github.com/Rugved76/todolist.git" target="blank">Github</a>
                </li>
            </ul>
        </div>
    </nav>
    <% if(taskitem!=null) { %>
        <div class="containe">
            <table>
                <!-- <tr>
                    <th id="heading">Tasks</th>
                    <th></th>
                </tr> -->
                <% taskitem.forEach(function(item){ %>
                    <tr>
                        <td class="x">
                            <%= item.taskname %>
                        </td>
                        <td>
                            <form method="post" action="/deleteSongs/<%= item._id %>">
                                <button type="submit"><strong class="y">⊗</strong></button>
                            </form>
                        </td>
                    </tr>
                <% }) %>
            </table>
        </div>
        <% } %>
</body>

</html>
